import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  task: {},
  activityIndicator: {
    paddingRight: 5,
  },

  doneButton: {
    marginRight: 15,
  },
});
export default styles;
