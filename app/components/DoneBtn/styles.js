import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  container: {
    marginRight: 16,
  },
  text: {
    fontWeight: '400',
    fontSize: 16,
    color: '#00AF4C',
  },
});

export default style;
